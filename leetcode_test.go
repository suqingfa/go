package leetcode

import (
	"testing"
)

func TestTable(t *testing.T) {
	tests := []struct {
		name   string
		output int
		input  int
	}{
		{},
	}

	for _, test := range tests {
		t.Run(test.name, func(t *testing.T) {
		})
	}
}
